# Legacy Streamlit Prototype (Deprecated)

This `src/` tree contains the old Streamlit prototype implementation and is no longer the active runtime path.

Active platform implementation:

- `zoswi-ai-interview/backend` (FastAPI + workers)
- `zoswi-ai-interview/frontend` (Next.js)

Do not use `src/` for new development.

